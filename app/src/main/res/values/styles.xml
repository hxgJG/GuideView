<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <item name="colorPrimaryDark">@color/app_color</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="button_style">
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/button_padding_size</item>
        <item name="android:background">@drawable/button_round_bg</item>
        <item name="android:textColor">@color/withe</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

</resources>
